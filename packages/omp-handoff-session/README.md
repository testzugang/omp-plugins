# omp-handoff-session

OMP extension to create focused handoffs for new sessions.

## Usage

```text
/handoff-session [goal]
```

Saved handoff files are contained under `docs/omp/handoffs/`. Model settings use
`getAgentDir()` for user settings and `.omp/settings.json` for project settings.
